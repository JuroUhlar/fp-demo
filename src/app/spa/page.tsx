import { SpaDemo } from '../../components/SpaDemo';
import { MAIN_SUB_LOAD_OPTIONS } from '../../constants';

export default function ExamplePage() {
  return <SpaDemo {...MAIN_SUB_LOAD_OPTIONS} name="SPA Package demo All Smart signals" />;
}
